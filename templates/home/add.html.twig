{% extends "base.html.twig" %}

    {% block head %}
        <meta charset="UTF-8">
        <title>{% block title %}yassine{% endblock %}</title>


    {% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col s12 m12 l12">
                <h4>Ajouter Perssone</h4>
                {{ form_start(form) }}

                {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary"> Enregister  </button>

        {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
